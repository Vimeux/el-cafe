{% extends 'base.html.twig' %}

{% block title %}Coffee{% endblock %}

{% block body %}
    <h1>Coffee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coffee.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ coffee.name }}</td>
            </tr>
            <tr>
                <th>Intensity</th>
                <td>{{ coffee.intensity }}</td>
            </tr>
            <tr>
                <th>Origin</th>
                <td>{{ coffee.origin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_coffee_index') }}">back to list</a>

    <a href="{{ path('app_coffee_edit', {'id': coffee.id}) }}">edit</a>

    {{ include('coffee/_delete_form.html.twig') }}
{% endblock %}
