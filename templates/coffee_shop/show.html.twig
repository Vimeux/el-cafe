{% extends 'base.html.twig' %}

{% block title %}CoffeeShop{% endblock %}

{% block body %}
    <h1>CoffeeShop</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coffee_shop.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ coffee_shop.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ coffee_shop.address }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ coffee_shop.website }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_coffee_shop_index') }}">back to list</a>

    <a href="{{ path('app_coffee_shop_edit', {'id': coffee_shop.id}) }}">edit</a>

    {{ include('coffee_shop/_delete_form.html.twig') }}
{% endblock %}
