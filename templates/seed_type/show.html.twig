{% extends 'base.html.twig' %}

{% block title %}SeedType{% endblock %}

{% block body %}
    <h1>SeedType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seed_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ seed_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_seed_type_index') }}">back to list</a>

    <a href="{{ path('app_seed_type_edit', {'id': seed_type.id}) }}">edit</a>

    {{ include('seed_type/_delete_form.html.twig') }}
{% endblock %}
